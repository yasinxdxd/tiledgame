import "../tiled.tile";

func main(argc: int): int {

	if (abs(x) < 1 &&
	abs(y) < 1 &&
	abs(z) < 3) {return RED;}
	
	if (abs(x) < 2 &&
	abs(y) < 2
	&& abs(z) < 3) {return WHITE;}
	
	if (abs(x) < 3 &&
	abs(y) < 3 &&
	abs(z) < 3) {return GREEN;}
	
	
	return EMPTY;

}